import { notFound } from "next/navigation";

// Simulateur IA supprimé (AUDIT.md §11).
// La page renvoie 404 en attendant sa refonte non-IA.
export default function SimulateurPage() {
  notFound();
}
